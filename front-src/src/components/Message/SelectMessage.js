import React from "react";
import { FlexboxGrid } from "rsuite";

function SelectMessage() {
    return (
        <div>
            <FlexboxGrid justify="center">
                <h3 style={{ fontFamily: "'Palatino Linotype', 'Book Antiqua', Palatino, serif", color: "#7986CB", textAlign: "center" }}>Select Pic</h3>
            </FlexboxGrid>
        </div>
    );
}

export default SelectMessage;
